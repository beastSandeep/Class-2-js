// getBoundingClientRect() is a element's method which tells us accurate postion and width or height like crutial informations

const btn = document.getElementById("btn");

const rect = btn.getBoundingClientRect();
console.log(rect);
